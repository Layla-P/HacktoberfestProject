<ul class="navbar-nav">
    @if (!User.Identity.IsAuthenticated)
    {
        <a asp-action="Login" asp-controller="Auth" class="btn btn-outline-primary">Log In with GitHub</a>

    } else
    {
        <a asp-action="Logout" asp-controller="Auth" class="btn btn-outline-primary">Logout</a>
    }
      
</ul>